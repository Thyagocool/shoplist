# Para rodar o frontend LOCALMENTE (fora do Docker):
# Copie este arquivo para .env e descomente a linha abaixo
# VITE_API_URL=http://localhost:8000/api/v1
#
# Dentro do Docker o proxy do Vite resolve automático, não precisa de .env
